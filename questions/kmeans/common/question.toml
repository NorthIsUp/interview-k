# What the CoderPad bank calls this question. coderpad.toml keys its ids by title and there is
# no server-side lookup by title, so renaming here orphans the existing question ids.
title       = "k-means"
description = "Implement clustering from scratch. Scored against RUBRIC.md in the interview-k repo."
